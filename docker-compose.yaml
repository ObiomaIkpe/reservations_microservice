services:
  reservations:
    build:
      context: .
      dockerfile: ./apps/reservations/Dockerfile
      target: development
    command: pnpm run start:dev reservatio ns
    volumes:
      - .:/usr/src/app
    ports:
      - "3000:3000"
  mongo:
    image: mongo